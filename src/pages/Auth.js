import React from 'react';
import AuthForm from '../components/AuthForm';

const Auth = () => {
    return (
        <React.Fragment>
            <AuthForm />
        </React.Fragment>
    );
};

export default Auth;